version: 1
applications:
  - backend:
      phases:
        build:
          commands:
            - '# Push Amplify backend (AppSync/GraphQL, functions, auth, storage)'
            - 'echo "Skipping amplifyPush in CI (disabled in repo). If you need backend push, run amplifyPush locally or enable this step."'
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
